import { NgxInputDirective } from "./ngx-input.directive";

describe("NgxInputDirective", () => {
	it("should create an instance", () => {
		const directive = new NgxInputDirective();
		expect(directive).toBeTruthy();
	});
});
