import { NgxInputDirective } from "libs/ngx-textarea/src/lib/directive/ngx-textarea.directive";

describe("NgxInputDirective", () => {
	it("should create an instance", () => {
		const directive = new NgxInputDirective();
		expect(directive).toBeTruthy();
	});
});
